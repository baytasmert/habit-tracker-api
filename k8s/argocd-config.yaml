apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-cmd-params-cm
  namespace: argocd
data:
  server.insecure: "true"      # nginx ingress HTTP üzerinden konuşabilsin
  server.rootpath: "/argocd"   # localhost/argocd altında servis edilsin
